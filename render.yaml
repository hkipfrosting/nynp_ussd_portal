services:
  - type: web
    name: ussd-school-portal
    runtime: php
    region: ohio
    plan: free
    buildCommand: 'echo "No build needed"'
    startCommand: 'php -S 0.0.0.0:$PORT -t .'
    envVars:
      - key: PORT
        value: 10000
